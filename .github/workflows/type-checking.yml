name: Type Checking

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  type-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0

      - name: Install package with dev dependencies
        run: uv sync --extra dev

      # Two invocations rather than `mypy src tests`: the app is reachable as both
      # `app.*` (via `mypy_path = "src"`) and `src.app.*` (how the tests import it), and
      # checking both roots at once makes mypy refuse with "source file found twice
      # under different module names".
      - name: Run mypy on src
        run: uv run mypy src --config-file pyproject.toml
        env:
          ENVIRONMENT: local
          SECRET_KEY: test-secret-key-for-testing-only

      - name: Run mypy on tests
        run: uv run mypy tests --config-file pyproject.toml
        env:
          ENVIRONMENT: local
          SECRET_KEY: test-secret-key-for-testing-only

      # `scripts/` imports nothing from the app, so it needs neither the env nor its own
      # invocation for the module-name reason above - it is separate only because it is not
      # under either root.
      - name: Run mypy on scripts
        run: uv run mypy scripts --config-file pyproject.toml
